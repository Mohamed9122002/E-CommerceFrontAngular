import { PipesSearchPipe } from './search.pipe';

describe('PipesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
